# chamber_roommates

# Cloning into the Repo
Using HTTPS: `git clone https://github.com/mei-ame/chamber_roommates.git`

# GUI Instructions

# Input File Formatting
Download the results file from the form ***as a csv***, upload it as is to the GUI.

# Ensure you have the packages from the requirements.txt installed:
pip install -r requirements.txt

pulp, pandas, numpy, tkinter